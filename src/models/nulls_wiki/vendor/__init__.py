"""Vendored NULLs (SeqTD) inference code. Provenance and edits: NOTICE.md.

``masking`` is import-safe without litgpt; ``seqtd_model``/``seqtd_config``
require the ``nulls`` dependency group (litgpt) and are imported lazily by
the backend.
"""
